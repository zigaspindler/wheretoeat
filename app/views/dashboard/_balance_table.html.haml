.panel.panel-default
  .panel-heading
    %h3.panel-title
      Balance table
  %table.table
    %tr
      %th Name
      %th.text-right Balance
    - @balances.each do |balance|
      %tr
        %td= balance[:name]
        %td.text-right= (balance[:balance] / 100.0).to_s + ' €'
